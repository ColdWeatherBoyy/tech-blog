<div>
	<section class="card">
		<a href="/blogposts/{{id}}">
			<header class="centered"><strong>{{title}}</strong></header>
			<div class="blog-info">
				<span class="left">By {{user.name}} </span>
				<span class="right">Posted on: {{format_date created_at}}</span>
			</div>
			<p class="card-body">{{post}}</p>
		</a>
	</section>
</div>